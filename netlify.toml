[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  SECRETS_SCAN_SMART_DETECTION_ENABLED = "false"

# SPA redirect - handle all routes with React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
